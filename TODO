# Todo

* Add support for x86_64 linux and windows via nasm.
* Should users be able to customize the assembler they choose(TBD)?
* Use ld linker for linking.
* Have an interface for creating and executing process between windows and linux.
* Have tester.c with its own test framework with sha256 and custom file format
* Make the linker and assembler bakeable programs. If user explicitly requires it from the CLI config.
* Add support for wasm.
* Libclang parsing should only be added as a compiler module. Hard depending on build time is stupid.
* Majority of the directives feature do not work yet. We will add support for each one.
* Make the comptime block interpreterd and also uses byte code.

# Sat Jul 11 10:36:25 2026

-> Refer the NOTES in the tests/ directory
